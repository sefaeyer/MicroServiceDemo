server:
  port: 8090

spring:
  application:
    name: config

  profiles:
    active:
      - native # diger uygulamalarin config ayarlarini config dizininden import edilmesini saglar

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8097/eureka  # !!! eureka nin adresini yaziyoruz
